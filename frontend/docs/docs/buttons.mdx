---
title: Кнопки
slug: /
---
import { Sandbox } from '../src/components/Sandbox/Sandbox'

Кнопка используется как триггер для выполнения определенного действия. Кнопка однозначно
сообщает пользователю, что произойдет после нажатия на неё.

**Когда используется**

Основная кнопка (primary)
- когда необходимо обозначить ключевое или страртовое действие на странице (например, начать заполнять форму)
- в диалоговом окне, когда интерфейс предлагает совершить те или иные альтернативные действия

Второстепенная кнопка (secondary)
- когда компонент не должен привлекать повышенного внимания или когда место на странице ограничено для размещения,
а использование ссылки не подходит
- когда возможны два варианта действия, одно из которых ключевое и выполнено основной кнопкой (например в диалоге
модального окна)

### Цвета кнопок
<Sandbox projectId="jZhf2" />

### Контурные кнопки
<Sandbox projectId="fHsqE"/>

### Размеры кнопок
<Sandbox projectId="LfN3L"/>

### Кнопка с выпадающем списком
<Sandbox projectId="jMw1K"/>

### Кнопки с иконкой
<Sandbox projectId="lh3yC"/>

### Скругленные кнопки
<Sandbox projectId="g8C6G"/>

### Группа кнопок
<Sandbox projectId="bBNvT"/>

### Расположение кнопок
<Sandbox projectId="DPo1w"/>

### Кнопка с подтверждением
<Sandbox projectId="fyb9v"/>

