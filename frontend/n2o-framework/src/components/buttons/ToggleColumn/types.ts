export interface Props {
    // eslint-disable-next-line react/no-unused-prop-types
    id: string
    icon?: string
    label?: string
    entityKey: string
    defaultColumns: string
    locked: string
    nested: string
}
