version: "3.6"

services:
  frontend:
    ports:
      - "8710:8080"
    environment:
      - SERVER_SERVLET_CONTEXT_PATH=/latest/demo
  storybook:
    ports:
      - "8712:80"
  docusaurus:
    ports:
      - "8713:80"
