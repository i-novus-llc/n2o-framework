{
  "config": 1234,
  "test": {
    "inner-test": 4321
  },
  "user": {
    "username": "#{username}",
    "roles": "#{roles}",
    "age": "#{age}",
    "isActive": "#{isActive}",
    "combined": "test#{value}"
  },
  "context": "#{testContext}",
  "prop": "${testProp}"
}