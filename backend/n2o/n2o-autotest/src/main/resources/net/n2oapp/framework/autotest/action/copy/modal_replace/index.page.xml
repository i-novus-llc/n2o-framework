<?xml version='1.0' encoding='UTF-8'?>
<page xmlns="http://n2oapp.net/framework/config/schema/page-4.0">
    <datasources>
        <datasource id="formDS"/>
    </datasources>
    <regions>
        <form id="form" datasource="formDS">
            <fields>
                <input-text id="address" label="Адрес">
                    <toolbar>
                        <button label="Ввести новый" model="filter">
                            <show-modal page-id="modal" submit-action-type="copy" copy-mode="replace" copy-datasource="modalDS" target-datasource="formDS"/>
                        </button>
                    </toolbar>
                    <dependencies>
                        <set-value on="city,street">var address = "";
                            if (typeof city != 'undefined')
                            address += city;
                            if (typeof street != 'undefined') {
                            if (address.length !== 0)
                            address += ", ";
                            address += street;
                            }
                            return address;</set-value>
                    </dependencies>
                </input-text>
            </fields>
        </form>
    </regions>
</page>