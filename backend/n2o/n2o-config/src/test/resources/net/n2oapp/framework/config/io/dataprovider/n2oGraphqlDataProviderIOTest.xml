<graphql xmlns="http://n2oapp.net/framework/config/schema/data-provider-1.0" endpoint="http://localhost:8080/graphql">
    query getAllPeople($first: Integer, $last: Integer) {
        allPeople(first: $first, last: $last) {
            people {
                id
                name
                gender
                eyeColor
            }
            totalCount
        }
    }
</graphql>